song = render partial: "song.json.jb", locals: {song: @song}

song[:user] = {
  artist_name: @song.user.artist_name,
  profile_picture: @song.user.profile_picture
}

song[:comments] = @song.comments.map do |comment|
  render partial: "/api/comments/comment", locals: {comment: comment}
end 

song
